# Updated requirements — compatible with the current package ecosystem (September 2026).
# Minimum Streamlit 1.49.0 is required because app.py uses the string-based
# dataframe width API (width="stretch"), introduced in Streamlit 1.49.
streamlit>=1.49.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
openpyxl>=3.1.0
